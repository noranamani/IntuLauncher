# システム要件定義書：IntuLauncher AIエンジン (v1.0)

## 1. システム概要
本システムは、Android OSのイベントおよびユーザーの操作履歴をリアルタイムで解析し、ユーザーの「次の意図」を予測してホーム画面を動的に再構成する。ユーザーによる手動設定を不要とし、デバイスがユーザーの文脈（Context）に適応することを目的とする。

## 2. 機能要件

### 2.1 コンテキスト・モニタリング機能
システムは以下の外部イベントおよび状態を常に監視し、特徴量として抽出する。
* **ハードウェア状態:**
    * オーディオ接続（有線イヤホン、BluetoothデバイスのID判別）
    * 充電状態（AC、USB、ワイヤレス、バッテリー残量）
* **センサー・位置情報:**
    * 現在座標（自宅/職場/その他の判定）
    * アクティビティ認識（静止/歩行/走行/乗車）
* **時間・カレンダー:**
    * 時間帯、曜日、祝祭日
    * Googleカレンダー等の予定（キーワード抽出：例「会議」「フライト」）

### 2.2 ユーザー行動学習機能 (Context-Action Learning)
* **イベントトリガー学習:**
    特定のイベント（例：イヤホン接続）が発生した後、60秒以内に起動されたアプリを記録し、相関関係を定義する。
* **頻度・順序分析:**
    同一コンテキスト下でのアプリ起動の成功確率を算出。
    * *例：Bluetooth「WF-1000XM5」接続時 ＝ YouTube起動（確率85%）*
* **忘却アルゴリズム:**
    過去の古い習慣が現在の行動を邪魔しないよう、一定期間利用されなかった相関関係の重みを自動的に減衰させる。

### 2.3 動的インターフェース制御機能
* **3スロット・リコメンデーション:**
    予測確率が高い上位3つのアプリを、ホーム画面のメイン領域に即座に配置する。
* **ダイレクト・アクション（自動実行）:**
    予測確率が極めて高い（例：95%以上）かつ、ユーザーが許可した特定の組み合わせにおいて、アプリをバックグラウンドで事前ロード、またはフォアグラウンドで自動起動する。
* **アンビエント背景変更:**
    学習された「仕事モード」「移動モード」等の判定に基づき、UIテーマ（カラー、透過度）を自動変更する。

## 3. 非機能要件

### 3.1 プライバシーとセキュリティ (Edge AI)
* **ローカル処理の原則:**
    全ての行動ログおよび学習モデルはデバイス内のセキュアな領域に保存し、外部サーバーへの送信を一切禁止する。
* **データの透明性:**
    ユーザーが学習された相関関係を確認し、特定の履歴を削除できるインターフェースを提供する。

### 3.2 パフォーマンス
* **低消費電力:**
    コンテキスト解析はAndroidの「JobScheduler」や「WorkManager」を最適に活用し、バッテリー消費を最小限に抑える。
* **リアルタイム性:**
    イベント発生（例：イヤホン接続）からホーム画面の書き換え完了までを500ms以内に行う。

## 4. ユースケース例

| トリガーイベント | 判定コンテキスト | 推論結果（配置アプリ） | 備考 |
| :--- | :--- | :--- | :--- |
| Bluetooth接続 | 外出中・移動中 | YouTube, Spotify, 地図 | 通勤・通学と判断 |
| 座標:自宅＋23:00 | 静止・充電中 | アラーム, 瞑想, Kindle | 就寝準備と判断 |
| カレンダー「会議」 | 職場・開始5分前 | Slack, カレンダー, ボイスレコーダー | 仕事モードへ自動切替 |


# IntuLauncher 進化アイディア：拡張要件定義案 (v1.1)

## 1. マイクロ・ウィジェット自動生成 (Deep Action)
アプリを「起動する」段階から、アプリ内の「機能を実行する」段階へ進化させる。

*   **機能概要:** アプリアイコンの代わりに、特定のタスク（Deep Link）を直接スロットに配置。
*   **具体例:**
    *   **メディア:** YouTubeアイコンではなく「お気に入りプレイリストの再生」ボタンを表示。
    *   **決済/実用:** 駅の座標検知時に「Suica残高」を表示し、スーパー付近では「ポイントカード」を直接表示。
    *   **コミュニケーション:** 特定の時間に頻繁に連絡する相手がいる場合、その相手への「返信ボタン」を配置。

## 2. フィジカル・コンテキスト認識 (Physical Context)
デバイスの物理的な状態から、ユーザーの無意識の意図を読み取る。

*   **持ち方・角度の検知:**
    *   **縦持ち vs 横持ち:** ポケットから取り出して即座に「横」に構えた場合、カメラまたは動画視聴モードと判断。
    *   **伏せ置き解除:** 画面を伏せた状態からの復帰直後は、未読通知の確認や仕事用アプリを優先。
*   **歩行リズム解析:**
    *   **早歩き/急ぎ:** 操作ミスを防ぐため、UI要素を大型化し、地図や交通機関情報に絞り込む。
    *   **静止:** じっくり閲覧するニュースやSNSの優先度を上げる。

## 3. セレンディピティ・スロット (Discovery Engine)
効率化の副作用である「体験のマンネリ化」を防ぐため、あえて予測を外した提案を行う。

*   **セレンディピティ（偶然の発見）枠:** 3つのスロットのうち1つを、定期的に「過去の習慣」や「周辺の新しい発見」に割り当てる。
*   **具体例:**
    *   **リバイバル:** 1年前の同時期に頻繁に使用していたアプリ（季節性のある趣味アプリ等）を提示。
    *   **周辺探索:** 現在地付近で、ユーザーの興味関心に近いスポットのショートカット（写真撮影、口コミ等）を提案。

## 4. 進化ロードマップ (Roadmap)

| フェーズ | フォーカス | 実装のポイント |
| :--- | :--- | :--- |
| **Phase 1: 効率化** | 行動履歴の学習 | コンテキストに基づくアプリ配置の精度向上 |
| **Phase 2: 深度化** | アプリ内連携 | マイクロ・ウィジェットによる「0タップ」操作の実現 |
| **Phase 3: 情緒化** | 身体性と発見 | 持ち方の検知や、偶然の出会いを提供するUIへの進化 |

## 5. 設計思想の補足
「ユーザーが次に何をしたいか」を当てるだけでなく、**「ユーザーがまだ気づいていない、次にしたいこと」**を提示することで、デバイスとの信頼関係を深化させる。

## 6. コールドスタート（初期導入時）のUX設計【実装済み】

### 6.1 既存統計のインポート
* **UsageStats APIの利用:** インストール以前のアプリ利用頻度・累積時間を取得し、初回起動時の3スロット配置に反映する。
* **初期配置アルゴリズム:** 統計的に「最も利用頻度が高いアプリ」をデフォルトとして配置。

### 6.2 学習プロセスの可視化
* **AI学習ステータス:** 学習が不十分な期間（例：最初の3日間）は、AIスロットに「Learning...」等の控えめなエフェクトを表示し、ユーザーに「これから最適化される」という期待感を醸成する。
* **フィードバックループ:** AIが配置したアプリを使わずに検索から別のアプリを起動した場合、それを「負のフィードバック」として即座に学習に反映する。

### 6.3 属性ベースのプリセット
* **プロファイル選択:** 初回起動時に「ビジネス」「学生」「エンタメ重視」などのプロファイルを選択させることで、学習ゼロの状態でも確度の高い初期配置を実現する。

## 7. 導入支援・コンバージョン戦略【実装済み】

### 7.1 プログレッシブ・マイグレーション (段階的移行)
* **ウィジェット・トライアル:** 既存ホーム上で動作する「予測ウィジェット」を提供。AIの予測精度を「購入前」に体験させ、コンバージョン率を高める。
* **配置リスペクト:** 旧ホームの配置パターンを解析し、UIの重心（右利き/左利き、アイコン密度）を初期設定に反映する。

### 7.2 ベネフィットの可視化 (ROI Reporting)
* **Time-Saved ダッシュボード:** アプリ検索・スワイプ回数の削減量を数値化し、「スマホ操作の効率化」を実感させるレポート機能。
* **検索不要体験の強調:** 「最後にアプリ一覧（ドロワー）を開いたのは◯日前です」という通知を送り、AIの有効性を再認識させる。

### 7.3 インタラクティブ・プロファイリング
* **ゲーミフィケーション設定:** 5つの質問にスワイプで答えるだけで、AIが初期モデルを生成する「1分間セットアップ」。
* **ビジュアル・プレビュー:** 設定変更を保存する前に、数時間後の自分のホーム画面がどう変化するかをシミュレーション表示する。

## 8. ウィジェット先行型オンボーディング・フロー【実装済み】

### 8.1 段階的学習プロセス (Stealth Learning)
* **導入形態:** 既存ホームアプリ用ウィジェットとしてリリース。
* **学習ログ蓄積:** ウィジェットとして動作中も、本ランチャー採用時と同等のコンテキスト解析・行動学習を実施。
* **ユーザー体験:** 予測精度の向上をウィジェット内のアイコン変化として視覚的に示し、「AIの賢さ」を段階的に理解させる。

### 8.2 移行トリガー (Conversion Trigger)
* **的中率ベース:** ウィジェット内提示アプリのタップ率が閾値を超えた際、フル機能版への移行を提案。
* **ベネフィット・プレゼンテーション:** 蓄積されたデータから「ホーム全体を最適化した場合の時短効果」をシミュレーション表示。

### 8.3 移行後の心理的安全性
* **クイック・ロールバック:** 移行後、一定期間は「元のホームアプリ設定へ戻す」ショートカットを常設。
* **環境維持:** 旧ホームでのウィジェット配置場所を起点に、メインUIを構成するオプションを提供。

## 9. アンビエント・ビジュアル（背景自動最適化）【実装済み】

### 9.1 コンテキスト・シンクロ・ダイナミクス
* **環境サンプリング:** 現在地の天候、時刻、照度センサーの情報を取得し、UIテーマカラーと背景画像をリアルタイムで生成・更新する。
* **カラーセマンティクス:** 
    * 集中が必要な文脈（カレンダー予定等）では、低彩度の背景を選択。
    * アクティブな時間帯（移動中等）では、高コントラストで視認性を重視。

### 9.2 パーソナル・メモリー・ウォールペーパー
* **フォトライブラリ連携:** ユーザーの過去の写真から、現在の位置情報や季節性に合致するものを抽出。
* **プライバシー保護加工:** 写真をそのまま出すのではなく、AIによる抽象化フィルタ（ブラー、油彩化等）をかけ、ホーム画面の視認性を維持しつつ「懐かしさ」を演出する。

### 9.3 集中度連動型ビジュアルノイズ制御
* **動的ブラー（ぼかし）:** アプリの使用頻度が高まっている時や、通知が多い時は背景のディテールを自動でぼかし、ユーザーの認知負荷を軽減する。

## 10. 背景モード・セレクター (Visual Flexibility)【実装済み】

### 10.1 デュアル・ビジュアル・システム
ユーザーは以下の2つのモードを、複雑な設定を介さず自由に選択・切り替え可能とする。
* **AI Ambient Mode:** 
    * 前述のコンテキスト同期・自動生成エンジンが駆動。
    * 常に「今の自分」に最適な空間を演出する。
* **Fixed Personal Mode:** 
    * ユーザーが指定した特定の画像を表示。
    * AIは背景の「変更」を停止し、代わりに背景画像に合わせてアイコンの影やフォントカラーを最適化する「視認性サポート」に専念する[cite: 1]。

### 10.2 インタラクティブ・トグル
* **シームレスな切り替え:** 画面のロングプレス、またはダブルタップにより、現在のAI生成背景と固定背景を即座にスイッチ可能。
* **AIへの学習フィードバック:** 固定モードが長く選択されている場合、AIは「今の画像に近い色調や雰囲気」をAIモードの生成アルゴリズムに反映させる。

### 10.3 視認性セーフガード
* どのような背景画像（白飛びした写真など）が指定されても、メインの3スロットとアンカースロットが埋もれないよう、AIが背後に適度なグラデーションやブラーを自動適用する。

## 11. インテリジェント・通知ハンドリング (Smart Notification)【実装済み】

### 11.1 エンゲージメント・ベース・フィルタリング
* **反応履歴の学習:** 各アプリの通知に対するユーザーの挙動（タップ、消去、放置）を記録。
* **自動ミュート:** 消去率が高い「不要な通知」と判断された場合、システム通知をバイパスし、サイレント状態で専用ログに格納する[cite: 1]。
* **通知の「鮮度」管理:** 時間経過とともに価値が下がる通知（例：期間限定クーポン）は、有効期限が切れる前に自動で整理する。

### 11.2 コンテキスト・プロンプト
* **アクション誘導:** タップ率が高い「重要な通知」が届いた際、ホーム画面のメインUI上に直接アクションボタン（例：LINEの返信、スマートロックの解錠など）をポップアップさせる[cite: 1]。
* **バッチ処理提案:** 特定の時間帯（例：昼休みや帰宅後）にまとめて通知を確認する傾向がある場合、その時間に「未読通知サマリー」を自動生成して提示する。

### 11.3 集中モードとの連動
* カレンダーの予定（会議中など）と連動し、緊急度の低い通知はAIが自動で保留。会議終了と同時に、重要なものから順に「振り返り」として提示する。

## 12. 技術的実現可能性とアーキテクチャ (Technical Feasibility)

### 12.1 デバイス内学習 (On-Device ML)
* **推論エンジン:** TensorFlow Lite または Android ML Kit を採用。
* **メリット:** 低遅延でのUI更新、およびプライバシーの完全保護（外部送信なし）を実現[cite: 1]。

### 12.2 システム権限の活用
* **Usage Access:** アプリ利用統計の取得に使用[cite: 1]。
* **Notification Access:** 通知のエンゲージメント学習とフィルタリングに使用[cite: 1]。
* **Accessibility Service (オプション):** 高度な自動化（特定アクションの自動実行）が必要な場合にのみユーザーの同意を得て利用[cite: 1]。

### 12.3 バッテリー最適化
* **Adaptive Updates:** センサーのサンプリング周期を動的に変更。静止時や就寝時はAIの再計算頻度を下げ、バッテリー消費を最小化する[cite: 1]。
