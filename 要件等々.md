# システム要件定義書：IntuLauncher AIエンジン (v1.0)

## 1. システム概要
本システムは、Android OSのイベントおよびユーザーの操作履歴をリアルタイムで解析し、ユーザーの「次の意図」を予測してホーム画面を動的に再構成する。ユーザーによる手動設定を不要とし、デバイスがユーザーの文脈（Context）に適応することを目的とする。

## 2. 機能要件

### 2.1 コンテキスト・モニタリング機能
システムは以下の外部イベントおよび状態を常に監視し、特徴量として抽出する。
* **ハードウェア状態:**
    * オーディオ接続（有線イヤホン、BluetoothデバイスのID判別）
    * 充電状態（AC、USB、ワイヤレス、バッテリー残量）
* **センサー・位置情報:**
    * 現在座標（自宅/職場/その他の判定）
    * アクティビティ認識（静止/歩行/走行/乗車）
* **時間・カレンダー:**
    * 時間帯、曜日、祝祭日
    * Googleカレンダー等の予定（キーワード抽出：例「会議」「フライト」）

### 2.2 ユーザー行動学習機能 (Context-Action Learning)
* **イベントトリガー学習:**
    特定のイベント（例：イヤホン接続）が発生した後、60秒以内に起動されたアプリを記録し、相関関係を定義する。
* **頻度・順序分析:**
    同一コンテキスト下でのアプリ起動の成功確率を算出。
    * *例：Bluetooth「WF-1000XM5」接続時 ＝ YouTube起動（確率85%）*
* **忘却アルゴリズム:**
    過去の古い習慣が現在の行動を邪魔しないよう、一定期間利用されなかった相関関係の重みを自動的に減衰させる。

### 2.3 動的インターフェース制御機能
* **3スロット・リコメンデーション:**
    予測確率が高い上位3つのアプリを、ホーム画面のメイン領域に即座に配置する。
* **ダイレクト・アクション（自動実行）:**
    予測確率が極めて高い（例：95%以上）かつ、ユーザーが許可した特定の組み合わせにおいて、アプリをバックグラウンドで事前ロード、またはフォアグラウンドで自動起動する。
* **アンビエント背景変更:**
    学習された「仕事モード」「移動モード」等の判定に基づき、UIテーマ（カラー、透過度）を自動変更する。

## 3. 非機能要件

### 3.1 プライバシーとセキュリティ (Edge AI)
* **ローカル処理の原則:**
    全ての行動ログおよび学習モデルはデバイス内のセキュアな領域に保存し、外部サーバーへの送信を一切禁止する。
* **データの透明性:**
    ユーザーが学習された相関関係を確認し、特定の履歴を削除できるインターフェースを提供する。

### 3.2 パフォーマンス
* **低消費電力:**
    コンテキスト解析はAndroidの「JobScheduler」や「WorkManager」を最適に活用し、バッテリー消費を最小限に抑える。
* **リアルタイム性:**
    イベント発生（例：イヤホン接続）からホーム画面の書き換え完了までを500ms以内に行う。

## 4. ユースケース例

| トリガーイベント | 判定コンテキスト | 推論結果（配置アプリ） | 備考 |
| :--- | :--- | :--- | :--- |
| Bluetooth接続 | 外出中・移動中 | YouTube, Spotify, 地図 | 通勤・通学と判断 |
| 座標:自宅＋23:00 | 静止・充電中 | アラーム, 瞑想, Kindle | 就寝準備と判断 |
| カレンダー「会議」 | 職場・開始5分前 | Slack, カレンダー, ボイスレコーダー | 仕事モードへ自動切替 |


# IntuLauncher 進化アイディア：拡張要件定義案 (v1.1)

## 1. マイクロ・ウィジェット自動生成 (Deep Action)
アプリを「起動する」段階から、アプリ内の「機能を実行する」段階へ進化させる。

*   **機能概要:** アプリアイコンの代わりに、特定のタスク（Deep Link）を直接スロットに配置。
*   **具体例:**
    *   **メディア:** YouTubeアイコンではなく「お気に入りプレイリストの再生」ボタンを表示。
    *   **決済/実用:** 駅の座標検知時に「Suica残高」を表示し、スーパー付近では「ポイントカード」を直接表示。
    *   **コミュニケーション:** 特定の時間に頻繁に連絡する相手がいる場合、その相手への「返信ボタン」を配置。

## 2. フィジカル・コンテキスト認識 (Physical Context)
デバイスの物理的な状態から、ユーザーの無意識の意図を読み取る。

*   **持ち方・角度の検知:**
    *   **縦持ち vs 横持ち:** ポケットから取り出して即座に「横」に構えた場合、カメラまたは動画視聴モードと判断。
    *   **伏せ置き解除:** 画面を伏せた状態からの復帰直後は、未読通知の確認や仕事用アプリを優先。
*   **歩行リズム解析:**
    *   **早歩き/急ぎ:** 操作ミスを防ぐため、UI要素を大型化し、地図や交通機関情報に絞り込む。
    *   **静止:** じっくり閲覧するニュースやSNSの優先度を上げる。

## 3. セレンディピティ・スロット (Discovery Engine)
効率化の副作用である「体験のマンネリ化」を防ぐため、あえて予測を外した提案を行う。

*   **セレンディピティ（偶然の発見）枠:** 3つのスロットのうち1つを、定期的に「過去の習慣」や「周辺の新しい発見」に割り当てる。
*   **具体例:**
    *   **リバイバル:** 1年前の同時期に頻繁に使用していたアプリ（季節性のある趣味アプリ等）を提示。
    *   **周辺探索:** 現在地付近で、ユーザーの興味関心に近いスポットのショートカット（写真撮影、口コミ等）を提案。

## 4. 進化ロードマップ (Roadmap)

| フェーズ | フォーカス | 実装のポイント |
| :--- | :--- | :--- |
| **Phase 1: 効率化** | 行動履歴の学習 | コンテキストに基づくアプリ配置の精度向上 |
| **Phase 2: 深度化** | アプリ内連携 | マイクロ・ウィジェットによる「0タップ」操作の実現 |
| **Phase 3: 情緒化** | 身体性と発見 | 持ち方の検知や、偶然の出会いを提供するUIへの進化 |

## 5. 設計思想の補足
「ユーザーが次に何をしたいか」を当てるだけでなく、**「ユーザーがまだ気づいていない、次にしたいこと」**を提示することで、デバイスとの信頼関係を深化させる。